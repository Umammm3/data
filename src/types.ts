export interface Item {
  id: string;
  code: string;
  name: string;
  date: string;
  description: string;
  createdAt: number;
}

export interface Module {
  id: string;
  title: string;
  link: string;
  createdAt: number;
}
